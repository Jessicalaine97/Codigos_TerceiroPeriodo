Tec. atuais: PHP

avaliacoes: trab final (3), trab. e teste (4), entrega semanal (2) e aps (1)

funciona do lado do servidor (nao compilavel)
docker compose up -d
tem que salvar os arquivos na public_html

<br> -> quebra linha (cod. html)
\n" -> espaco
. -> concatenar
-?= == <?php echo

http://localhost:8080/topico1/hello.php

rodar docker: docker-compose up -d

get manda informacoes direto na requisicao (link)  + rapido 
e post nao (os dois ) + seguro

qnd nao faz diferenca se é get ou post -> $_REQUEST

 <option value="RJ">Rio de Janeiro</option> -> util utilizar o value qnd se mostra um valor e se grava outro no banco